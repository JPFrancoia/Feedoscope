- Control rerun: rerun the kept `google/embeddinggemma-300m @ 2048`, `title_head`, `embedding_linear`, `TRAIN_BALANCE_MODE=full`, `C=5.0` configuration after the harness changes. If it no longer reproduces, stop and investigate before trusting new comparisons.
- `EmbeddingGemma classification prompt`: try `google/embeddinggemma-300m @ 2048`, `title_head`, `embedding_linear`, `TRAIN_BALANCE_MODE=full`, `EMBED_PROMPT_MODE=classification`, `EMBED_POOLING=mean`, `LINEAR_C=5.0`. This tests whether the model's classification-style prompt beats the plain control.
- `EmbeddingGemma document prompt`: try `google/embeddinggemma-300m @ 2048`, `title_head`, `embedding_linear`, `TRAIN_BALANCE_MODE=full`, `EMBED_PROMPT_MODE=document`, `EMBED_POOLING=mean`, `LINEAR_C=5.0`. This tests whether the title/document formatting from the model card helps article representations.
- `Alibaba-NLP/gte-large-en-v1.5 @ 2048`: try `title_head`, `embedding_linear`, `TRAIN_BALANCE_MODE=full`, `EMBED_POOLING=cls`, `EMBED_PROMPT_MODE=none`. This is the first larger dense baseline to test against the current EmbeddingGemma control.
- `Snowflake/snowflake-arctic-embed-l-v2.0 @ 2048`: try `title_head`, `embedding_linear`, `TRAIN_BALANCE_MODE=full`, `EMBED_POOLING=cls`, `EMBED_PROMPT_MODE=none`. This is the second larger dense baseline.
- `BAAI/bge-m3` hybrid: try `title_head`, `embedding_linear`, `TRAIN_BALANCE_MODE=full`, `EMBED_FEATURE_MODE=hybrid`, `EMBED_SPARSE_HASH_DIM=8192`, `EMBED_POOLING=mean`. This is the main sparse-vs-dense experiment because it combines semantic and lexical signals.
- `BAAI/bge-m3` sparse-only: try `title_head`, `embedding_linear`, `TRAIN_BALANCE_MODE=full`, `EMBED_FEATURE_MODE=sparse`, `EMBED_SPARSE_HASH_DIM=8192`. Only useful after the hybrid run succeeds.
- If a new prompt mode or larger dense model comes close to the control, only then sweep nearby `LINEAR_C` values. Do not start with a large hyperparameter sweep.
- If `bge-m3` sparse or hybrid mode fails because `FlagEmbedding` is missing, install it with `uv` and retry rather than discarding the mode.
